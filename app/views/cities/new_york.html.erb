hello. welcome to new york!

<%= form_for @city, url: {action: "create"} do |f| %>
  <%= f.text_field :name, placeholder: "city name" %>
  <%= f.text_field :state, placeholder: "state initials" %>
  <%= f.submit "Create" %>
<% end %>